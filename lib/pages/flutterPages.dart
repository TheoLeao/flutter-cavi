import 'package:flutter/material.dart';

class FlutterPages extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Page Sites", textScaleFactor: 2.0,)
    );
  }
}